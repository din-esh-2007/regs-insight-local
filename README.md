# regs-insight-local (Documentsaver)

Local copy of the Regs Insight project (Node/Express + MySQL).
Do NOT commit .env or uploads/ (they are in .gitignore).

## Quick start
1. Copy `.env.example` to `.env` and set DB + JWT.
2. npm install
3. node create_db.js
4. npm start

